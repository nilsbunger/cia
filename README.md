# CIA Multi-agent coding tool

## Set up:
`pnpm install`

## Running:
1. `pnpm start` (invoke from directory of the repo you're working on)

OR

2. Set up globally:
`pnpm link --global`
Then just run 'cia'
